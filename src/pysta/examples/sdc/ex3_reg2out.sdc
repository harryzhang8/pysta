# ==========================================================================
# 例 3：寄存器的 CP 端口 -> 输出端口
#
# 时钟周期 20ns。芯片外部那一段是 5.4ns，下游寄存器建立时间 1ns，
# 本设计里末级寄存器的 Tco 是 1ns，于是要综合的组合逻辑只剩下：
#
#     budget = Tclk - Tco - output_delay - uncertainty
#            = 20 - 1.0 - 6.4 - 0 = 12.6ns
#
# 注意 set_output_delay 的 6.4 是由两部分拼起来的：
#     外部组合逻辑 5.4 + 下游寄存器的 Tsu 1.0
# 也就是说，工具看不到的下游建立时间，也要一起算进 output_delay 里。
# ==========================================================================

create_clock -period 20 [get_ports clk]
set_output_delay -max 6.4 -clock clk [get_ports B]
