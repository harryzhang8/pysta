# ==========================================================================
# 例 5：多时钟 —— 输入端口
#
# 本设计内部只有 clkb（有实体端口），上游用的是 clka。
# clka 在本设计里没有对应端口，所以要用**虚拟时钟**来定义：
#     create_clock -name clka -period 30
# 虚拟时钟必须给 -name，因为它没有源端口可供命名。
#
# 公共基本周期 = LCM(30, 20) = 60ns。工具会在一个基本周期里遍历史所有发送沿：
#     发送沿 0ns  -> 捕获沿 20ns ：留给输入逻辑 20 - 5.5 - Tsu
#     发送沿 30ns -> 捕获沿 40ns ：留给输入逻辑 10 - 5.5 - Tsu   <-- 最严格
# 两条必须同时满足，取最严格的那条：
#     budget = 10 - 5.5 - 1.0 = 3.5ns
#
# 这就是“只看周期相减”会算错的地方 —— 必须同时看两个时钟的相位关系。
# ==========================================================================

create_clock -period 30 -name clka
create_clock -period 20 [get_ports clkb]

set_input_delay -max 5.5 -clock clka [get_ports A]
