# ==========================================================================
# 例 9b：多周期路径的**反例** —— 只写 -setup 6
# --------------------------------------------------------------------------
# 这是多周期约束里最容易踩的坑：
#
#   set_multicycle_path -setup 6 把建立检查推到第 6 个上升沿（60ns），
#   但同时也会把**保持检查**从“发送沿同沿”自动挪到“建立沿的前一沿”（50ns）。
#
#   于是工具拿到的约束变成：
#       组合逻辑延时 >= 50 + Th + uncertainty - Tco = 50.5ns   （保持）
#       组合逻辑延时 <= 60 - Tco - Tsu - uncertainty = 58ns    （建立）
#   它必须去凑一条延时落在 50.5 ~ 58ns 之间的路径。
#   可真实的组合逻辑只有零点几 ns —— 这个约束完全是在要求工具做无用功，
#   白白增加面积和功耗。
#
#   物理上的道理：能在 60ns 破坏采样的，是 0ns 那一沿发出的数据
#   （它走了 6 个周期才到），所以保持检查应该落回 0ns。
#
# 所以这个文件故意不加 -hold 5，用来和 ex9_multicycle_adder.sdc 对照。
# 跑一下就能看到 -49ns 量级的、毫无意义的保持违例。
# ==========================================================================

create_clock -period 10 [get_ports clk]

set_multicycle_path -setup 6 -to [get_pins FF3[*]/D]

# ↓↓↓ 少的就是这一行：它负责把保持沿从 50ns 拉回 0ns
# set_multicycle_path -hold 5 -to [get_pins FF3[*]/D]
